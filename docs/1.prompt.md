当前项目为[AutoShift-Planner]
已经考虑了下面条件：
核心框架: Nuxt + Vue 3
语言: TypeScript
样式方案: Unocss CSS
工具插件：Vueuse + Pinia
icon: Unocss CSS + @iconify-json/carbon
Auto-imports 机制
严格按我的代码风格写
请不要使用传统的diff + / - 格式。请用一种清晰的文本描述格式来展示代码变更，明确指出‘从哪一行开始’到‘哪一行结束’的旧代码块，应该被替换为新的代码块
下面回答全部使用中文回答


排班规则修改。
1.把随访下午/夜拆分开来（随访下午和随访夜）。
2.增加晚上档，晚上只能做随访夜（所以这里会出现许多人晚上休息的情况，注意这里是休息，不是休假）。
3.同一个人一天不能同时出现随访早上、随访下午和随访夜（所以现在每日必有随访上午/随访下午/随访夜/基础班）。
4.夜班防疲劳改为排随访夜后第二天上午不能排随访上午、舌苔评估和门诊
5.随访下午的工作量改为0.6，随访夜的工作量改为0.8

把当前任务的限制时段，以及工作量权重做成可配置

将工作量统计这个组件WorkloadDashboard 移除
放在 <ScheduleTable /> 下面，重新设计UI，仅展示差值，不需要展示是否分配不均

然后每个 可选排班方案 <ScheduleReadOnly :schedule="sch" /> 这个表格下面也放类似的 组件，来展示每个方案每个人的工作量

修改自动生成的逻辑，每次点击自动生成，仅将当前排班表中安排的门诊传入，其他任务都当作未设置传入，而不是现在将全部数据传入。
以便当我选择好一个方案后，想重新生成，但又要保留门诊排班的情况

优化一下生成算法，优先找到差值最小的方案，同时要根据不同的排班规则，比如我取消勾选了夜班防疲劳与上午防疲劳。也能从中找到最优路径，优先找到差值最小的方案。

将任务配置按钮放在排班规则说明这个组件中，同时添加本周排班表的导入导出功能。格式为 JSON

当前的自动生成算法，并不能在这有限的尝试次数里面找到成员差值最小的方案。比如我当前的门诊排班都（这里我取消了夜班防疲劳与上午防疲劳），每次生成都是最小成员差值: 2.0 的方案，但只要我把成员工作量大的把两天随访夜分别给另外两个成员，就能将最小成员差值缩小到0.2。同时符合规则。能不能解决这种问题

目前点击创建一个 会仅包含门诊的排班表副本作为算法基线 进行生成
现在改为弹出一个框，有两个选项，一个按当前排班表副本作为算法基线，一个按仅包含门诊的排班表副本作为算法基线

移除 排班规则说明 app/components/RulePanel.vue 中 hover 展示高亮格子的功能。
夜班防疲劳规则里面的规则单独可以配置
比如勾选了夜班防疲劳规则
但我只想要随访夜后面不能安排随访上午
或者
随访夜后面不能安排随访上午/舌苔评估
或者
随访夜后面不能安排随访上午/舌苔评估/门诊
把那三个选项单独列出来，勾选灵活一点

新增一条排班规则说明（可勾选，默认勾选），同一个人在当天安排有 `门诊` 或者`舌苔评估` 或者 `随访上午` 的任务情况下，不能安排 `随访夜`